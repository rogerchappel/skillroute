{
  "name": "skillroute",
  "version": "0.1.0",
  "description": "Deterministic skill routing plans for coding agents.",
  "type": "module",
  "bin": {
    "skillroute": "./src/cli.js"
  },
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src",
    "SKILL.md",
    "docs",
    "fixtures",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check.js",
    "build": "node scripts/build.js",
    "smoke": "node src/cli.js plan fixtures/catalog.json fixtures/tasks/repo-review.txt --format markdown",
    "package:smoke": "npm pack --dry-run",
    "release:check": "npm run check && npm test && npm run build && npm run smoke && npm run package:smoke"
  },
  "keywords": [
    "agent-skill",
    "agents",
    "skill-routing",
    "dry-run"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/skillroute.git"
  }
}
